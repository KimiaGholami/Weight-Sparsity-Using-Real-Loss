# Tested with: torch 2.13.0, transformers 5.14.1, datasets 5.0.0,
# Python 3.11.1, macOS 26.5.1 arm64 (Apple Silicon, CPU-only -- see
# README "Environment" section for why MPS is not used).
torch>=2.0
transformers>=4.30
datasets>=2.10
numpy
matplotlib
tqdm
